# ORGS evaluation — round 2 — 2026-04-19

Verdict: **PROMOTE**

Round 1 memo: `EVAL-20260419.md`. Round 1 verdict was ITERATE with
four specific tightenings. Round 2 re-evaluates after applying them.

## Epic goal, restated

Provide `orgs_add` and `orgs_update` RPCs so new forge orgs can be
onboarded and mutated without hand-editing `~/.config/hyperforge/orgs/`.

(Unchanged from round 1. Good — the goal is stable across iterations.)

## Supports

Enables `foyer` and every future project under a new forge org
(e.g. `juggernautlabs`) to be bootstrapped from pure RPC, without
text-editor detours.

(Unchanged from round 1.)

## Round 1 tightenings — status check

| # | Tightening | Applied? | Evidence |
|---|---|---|---|
| 1 | Pin `OrgSshKeys` struct (name, file, derives incl. `JsonSchema`) | ✅ | ORGS-2 "Pinned struct definition" section — struct body inline, file = `src/config/org.rs`, derives listed with per-derive rationale. |
| 2 | Pin `HyperforgeEvent` variants per case in ORGS-2 | ✅ | ORGS-2 "Event contracts per case" table — `Info`/`Error` variants with `type` tag and message substring contracts. |
| 3 | Add `workspace_path` three-state row to ORGS-3 | ✅ | ORGS-3 "`workspace_path` — three states, explicit" dedicated section with truth table. |
| 4 | Pin `HyperforgeEvent` variants per case in ORGS-3 | ✅ | ORGS-3 "Event contracts per case" table. |

All four applied as specified. No new items surfaced during the
revision pass.

## Per-ticket two-stranger assessment

| Ticket | Pass? | Notes |
|---|---|---|
| ORGS-1 (epic) | ✅ | Goal, supports, DAG, out-of-scope all pinned. No changes needed at round 2. |
| ORGS-2 (orgs_add) | ✅ | Struct pinned (name, file, derives). Event variants pinned with substring contracts. Acceptance criterion #8 forces test-level event verification (not "eyeball stdout"). Two strangers reading this now produce the same Done verdict. |
| ORGS-3 (orgs_update) | ✅ | References ORGS-2's struct rather than re-defining. Three-state `workspace_path` table is explicit. Event variants pinned. Criterion #10 tests each of the three states independently. |
| ORGS-4 (checkpoint) | ✅ | Unchanged from round 1 evaluation — `type: checkpoint`, two parts (automated + state map), paths updated to `artifacts/CHECKPOINT.md`. |

## DAG completeness

Unchanged from round 1: complete. Checkpoint `blocked_by` covers both
implementation tickets. No serial-could-be-parallel gaps. No `Pending`
ticket with an open design question inside.

## Scope discipline

Round 1 surfaced four critiques. All four classified as TIGHTEN, all
four applied in-place. No DEFER items disguised as TIGHTEN. No new
features added. The iteration preserved scope, which was the explicit
constraint for this round.

## Actions

**PROMOTE.** Ticket IDs ready to flip to `Ready` (human decision — this
memo is a recommendation, not an automatic flip):

- ORGS-2
- ORGS-3

ORGS-4 is `blocked_by: [ORGS-2, ORGS-3]` and promotes naturally after
those two reach `Complete`.

## Round budget remaining

Round 2 used. Round 3 available if any issues surface during
implementation start that require re-tightening. Under the skill's
budget, one more round is permitted before RE-SHAPE becomes the default
verdict.
